// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 15 中 swcMinify 已移除，默认启用
  // 删除 swcMinify 选项
  reactStrictMode: true,
  // 如果需要，添加其他配置
}

module.exports = nextConfig